name: Agentic CI/CD Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  evaluate:
    runs-on: ubuntu-latest

    services:
      # Optional: Start Chroma or other dependencies if needed
      # For now, we assume local persistence or mocked data for CI
      
    steps:
    - uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Run Evaluation Gate
      env:
        OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        OLLAMA_BASE_URL: ${{ secrets.OLLAMA_BASE_URL }} # If using a remote Ollama
        TEST_DATASET_PATH: test_dataset.json
        EVAL_CONFIG_PATH: eval_threshold_config.json
      run: python run_eval.py

    - name: Upload Evaluation Results
      if: always()
      uses: actions/upload-artifact@v3
      with:
        name: evaluation-results
        path: eval_results.json
